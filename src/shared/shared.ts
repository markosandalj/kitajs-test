import LocationAutocomplete from "./components/location-autocomplete/location-autocomplete";

export const sharedClassComponents = [
	{
		Class: LocationAutocomplete,
		selector: ".location-autocomplete",
	},
];

export const sharedWebComponents = [];
